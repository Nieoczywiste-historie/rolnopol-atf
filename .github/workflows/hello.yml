name: Hello from CI

on:
  push:
    branches:
      - Create-github-action
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello from CI"
